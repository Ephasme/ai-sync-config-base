name: Context7
description: Library documentation lookup via Context7.
method: stdio
command: npx
args: ["-y", "@upstash/context7-mcp"]
dependencies:
  env:
    CONTEXT7_API_KEY:
      local: {}
  binaries:
    - name: npx
      version:
        require: ~10.0.0
