name: Playwright
description: Browser automation and page inspection with Playwright.
method: stdio
command: npx
args: ["-y", "@playwright/mcp@latest"]
dependencies:
  binaries:
    - name: npx
      version:
        require: ~10.0.0
