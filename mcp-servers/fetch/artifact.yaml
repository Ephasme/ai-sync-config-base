name: Fetch
description: Read-only web content fetching from public URLs.
method: stdio
command: uvx
args: ["mcp-server-fetch"]
dependencies:
  binaries:
    - name: uvx
      version:
        require: ~0.10.0
